{
  "name": "hayden-trivia",
  "version": "1.0.0",
  "description": "A party game about the guest of honor: everyone writes what they know, Claude turns it into trivia.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "preview": "node scripts/preview.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0"
  }
}
